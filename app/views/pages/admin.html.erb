<div class="container">
  

  <div class="col-xs-8 col-xs-offset-2">
    <ul class="admin-buttons list">      
      <li><p><%= link_to 'New', admin_path %></p></li>
    </ul>
  </div>



  <div class="col-xs-8 col-xs-offset-2">
    <div class="post-style">
      <% @posts.each do |p| %>
        <h1 class="titles"><%= p.title %></h1></br>
        <p class="date"><%= p.created_at.strftime("%B %d, %Y") %></p>
      <% end %>
    </div>
  </div>
  
</div> 