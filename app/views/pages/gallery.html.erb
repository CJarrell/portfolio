<h1>Gallery</h1>

<% @projects.each do |project| %>

  <dl>
    <% project.each_pair do |key, value| %>
      <dt><%= key %></dt>
      <dd><%= value %></dd>
    <% end %> 
  </dl>
  <%= image_tag project["covers"]["404"] %>
<% end %> 
